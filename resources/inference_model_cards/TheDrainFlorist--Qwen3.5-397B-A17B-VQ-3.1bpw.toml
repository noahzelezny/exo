model_id = "TheDrainFlorist/Qwen3.5-397B-A17B-VQ-3.1bpw"
n_layers = 60
hidden_size = 4096
num_key_value_heads = 2
supports_tensor = true
tasks = ["TextGeneration"]
family = "qwen"
quantization = "vq-3.1bpw"
base_model = "Qwen3.5 397B A17B"
capabilities = ["text", "thinking", "thinking_toggle", "vision"]
reasoning_dialect = "post_last_user"
context_length = 262144
backends = ["MlxMetal"]
[storage_size]
in_bytes = 154277447647

# quantlab VQ artifact. Runs on STOCK mlx_lm via the bundled model.py
# (config.json model_file — patch_mlx_lm is retired). Tensor-parallel needs
# the codebook-replicate guard in auto_parallel.py. Vision tower grafted.

[sampling_defaults]
temperature = 0.6
top_p = 0.95
top_k = 20
min_p = 0.0
repetition_penalty = 1.0
presence_penalty = 0.0
