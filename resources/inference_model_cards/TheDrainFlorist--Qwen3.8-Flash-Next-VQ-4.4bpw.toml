model_id = "TheDrainFlorist/Qwen3.8-Flash-Next-VQ-4.4bpw"
n_layers = 48
hidden_size = 2560
num_key_value_heads = 2
supports_tensor = true
tasks = ["TextGeneration"]
family = "qwen"
quantization = "vq-4.4bpw"
base_model = "Qwen3.8 Flash Next"
capabilities = ["text", "thinking", "thinking_toggle", "vision"]
reasoning_dialect = "post_last_user"
context_length = 262144
backends = ["MlxMetal"]
[storage_size]
in_bytes = 101108822016

# VQLab VQ artifact (qwen4_exp), released 2026-08-30, HF twin of the local
# dir qwen4exp_vq_packed_92mix6. Self-contained: the VQ runtime ships in the
# checkpoint as model.py (config vq_modules/vq_ple), so mlx_lm loads it with
# trust_remote_code and NO patch files. Arch support still comes from mlx-lm
# PR #1788 (unmerged): qwen4_exp.py must exist in each node's exo-env mlx_lm
# — re-drop after any exo env rebuild. Tensor-parallel needs the codebook-
# replicate guard in auto_parallel.py (upstream PR #2268). Vision tower
# grafted bf16 (333 tensors); config carries vision_config + image_token_id.
# Geometry: d2/K256 experts + d8/K4096 PLE, hot-6 layers at d2/K1024 (10-bit packed).
# KL 50.3, top-1 92.8%. Fits 128GB boxes (94.1 GiB).

[sampling_defaults]
temperature = 0.6
top_p = 0.95
top_k = 20
min_p = 0.0
repetition_penalty = 1.0
presence_penalty = 0.0
